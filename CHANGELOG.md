# Changelog

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### Planned

- （暂无）

## [1.2.0] - 2026-07-27

### Added

- **可自定义状态标记**：设置页可配置标记左右边界、已完成文案、完成时是否加粗 `**`
- 设置页即时预览「抽取示例 / 完成后示例」
- 「恢复默认标记」按钮（【】+ 已完成 + 加粗）

### Changed

- 抽取与回写逻辑改为读取用户配置的标记规则，默认行为与 1.1.0 保持一致

## [1.1.0] - 2026-07-27

### Changed

- **抽取规则收紧**：仅当有序列表句首存在 `【…】` 状态标签时才进入看板；无标签的有序列表不再抽取
- 句首标签除 `【已完成】` 外（如 `【未完成】` / `【持续中】` / `【待观察】` 等）一律视为未完成待办
- **完成回写**：将句首 `【】` 内文字替换为已完成，并统一写成 `**【已完成】**`（加粗，便于主题高亮），不再在前面追加第二个 `【已完成】`

### Fixed

- 识别被 Markdown 加粗包裹的完成标记（如 `**【已完成】**`），避免已完成项被误抽入看板

## [1.0.0] - 2026-07-27

### Added

- 从指定日记文件夹（不递归）扫描 Markdown，抽取有序列表待办
- 侧边栏「日记待办看板」：重新编号、完成按钮、手动刷新
- 设置页：日记文件夹路径（必填）、附加文件夹（可选）
- 点击完成后写回原文状态标记；已完成项再次扫描不再出现
- 跳过代码块、引用块、无序列表
- 丝带图标与命令：打开看板 / 刷新看板
